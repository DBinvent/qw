[package]
name = "qw-node"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "QW node: local contact graph, per-contact relay policy, greedy referral-query routing"

[lib]
name = "qw_node"
path = "src/lib.rs"

[dependencies]
qw-protocol = { path = "../protocol" }
rand = "0.9"
serde_json = "1.0.151"
